import { useState } from 'react'
export default function Navbar(){const [open,setOpen]=useState(false);const links=['About','Skills','Projects','Experience','Education','Contact'];return <header className="nav"><a className="wordmark" href="#home" onClick={()=>setOpen(false)}>AM<span>/</span></a><button className="nav-toggle" aria-label="Toggle navigation" aria-expanded={open} onClick={()=>setOpen(!open)}><span/><span/></button><nav className={open?'nav-links nav-links--open':'nav-links'}>{links.map(x=><a key={x} href={`#${x.toLowerCase()}`} onClick={()=>setOpen(false)}>{x}</a>)}</nav><a className="availability" href="#contact"><i/> Available for select work</a></header>}
